import { Card, CardContent, CardHeader, CardTitle } from "../../../components/ui/card";

export default function CourseTeam() {
    return (
        <Card>
            <CardHeader>
                <CardTitle>Course Team</CardTitle>
            </CardHeader>
            <CardContent>
                <p className="text-muted-foreground">Course team management is not available yet.</p>
            </CardContent>
        </Card>
    );
}
