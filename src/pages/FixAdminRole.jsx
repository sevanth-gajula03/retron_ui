import { Card, CardContent, CardHeader, CardTitle } from "../components/ui/card";

export default function FixAdminRole() {
    return (
        <div className="space-y-6">
            <h1 className="text-3xl font-bold tracking-tight">Fix Admin Role</h1>
            <Card>
                <CardHeader>
                    <CardTitle>Fix Admin Role</CardTitle>
                </CardHeader>
                <CardContent>
                    <p className="text-muted-foreground">This utility is not available yet.</p>
                </CardContent>
            </Card>
        </div>
    );
}
